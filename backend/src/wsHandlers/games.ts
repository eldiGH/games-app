import type { RoomManager } from '../helpers/rooms';
import type { WsMessageHandler } from '../helpers/ws';

export const handleGame = (message: WsMessageHandler, roomsManager: RoomManager) => {
	///
};
