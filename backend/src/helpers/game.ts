import { WsMessageType } from '@shared/types';
import type { Room } from '../types';
import type { WsMessageHandler } from './ws';

export const handleGame = (message: WsMessageHandler, rooms: Room[]) => {};
